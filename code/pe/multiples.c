#include <stdio.h>
#include <stdlib.h>

static int multiple_of_x(const int x, const int num)
{
        return ((num % x) == 0);
}

static int multiple_of_x_and_y(const int x, const int y, const int num)
{
return 0;	
}

static int lcm(const int x, const int y)
{
	int grt = (x >= y) ? x : y;	
}

int main(void)
{
	return 0;
}
